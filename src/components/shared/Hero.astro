---
import { ArrowRight, Clock, MapPin } from "@lucide/astro";
import { Button } from "../ui/button";

import img from "@/assets/hero-img.png";
import { Image } from "astro:assets";
---

<section id="inicio" class="pt-32 pb-20 px-4">
  <div class="container mx-auto">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      {/* Content */}
      <div class="space-y-8">
        <div class="inline-block">
          <span
            class="bg-secondary/20 text-secondary-foreground px-4 py-2 rounded-full text-sm font-semibold"
          >
            🔥 Feitos fresquinhos todos os dias
          </span>
        </div>

        <h1
          class="text-5xl md:text-6xl lg:text-7xl font-bold leading-tight text-balance"
        >
          Salgados <span class="text-primary">artesanais</span> que derretem na boca
        </h1>

        <p class="text-xl text-muted-foreground leading-relaxed text-pretty">
          Tradição mineira em cada mordida. Feitos com ingredientes selecionados
          e muito carinho, nossos salgados são perfeitos para sua festa, evento
          ou aquele lanche especial.
        </p>

        <div class="flex flex-col sm:flex-row gap-4">
          <Button
            size="lg"
            className="bg-primary hover:bg-primary/90 text-primary-foreground text-lg h-14 px-8"
          >
            Ver Cardápio
            <ArrowRight class="w-5 h-5 ml-2" />
          </Button>
          <Button
            size="lg"
            variant="outline"
            className="text-lg h-14 px-8 border-2 bg-transparent"
          >
            Fale Conosco
          </Button>
        </div>

        {/* Info Cards */}
        <div class="grid sm:grid-cols-2 gap-4 pt-4">
          <div
            class="flex items-start gap-3 p-4 bg-card rounded-lg border border-border"
          >
            <Clock class="w-6 h-6 text-primary flex-shrink-0 mt-1" />
            <div>
              <p class="font-semibold text-card-foreground">Horário</p>
              <p class="text-sm text-muted-foreground">Seg-Sáb: 7h às 19h</p>
            </div>
          </div>
          <div
            class="flex items-start gap-3 p-4 bg-card rounded-lg border border-border"
          >
            <MapPin class="w-6 h-6 text-primary flex-shrink-0 mt-1" />
            <div>
              <p class="font-semibold text-card-foreground">Localização</p>
              <p class="text-sm text-muted-foreground">Uberaba, MG</p>
            </div>
          </div>
        </div>
      </div>

      {/* Image */}
      <div class="relative">
        <div class="aspect-square rounded-3xl overflow-hidden bg-muted">
          <Image
            src={img}
            alt="Salgados artesanais"
            class="w-full h-full object-cover"
            loading="eager"
          />
        </div>
        {/* Floating Badge */}
        <div
          class="absolute -bottom-6 -left-6 bg-secondary text-secondary-foreground p-6 rounded-2xl shadow-xl"
        >
          <p class="text-4xl font-bold">15+</p>
          <p class="text-sm font-semibold">Variedades</p>
        </div>
      </div>
    </div>
  </div>
</section>
